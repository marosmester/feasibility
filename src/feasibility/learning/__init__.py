"""Learning glue shared between the two submodules' demos: pose-error metrics
(pose_error.py), PyTorch data loading over comparator/ output HDF5 (custom_dataset.py), a
generator for that HDF5 (generate_dataset.py), a per-sample error viewer over one such file
(error_visual.py), and the MLP that regresses those errors (model.py)."""
