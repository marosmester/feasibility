"""Learning glue shared between the two submodules' demos: pose-error metrics
(pose_error.py), PyTorch data loading over comparator/ output HDF5 (custom_dataset.py), and a
generator for that HDF5 (generate_dataset.py)."""
