"""Grid-based ostrich-vs-helhest_stack divergence dataset + model -- see generate_dataset.py's
module docstring for the dataset schema and ARCHITECTURE.md for the network design.

Deliberately independent of feasibility.learning (see generate_dataset.py's module docstring for
why) -- this package restates rather than imports that tree's constants/formulas.
"""
